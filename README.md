# CANBUS-Schedule

An STM32 project implementation for the task of CAN bus message scheduling, for the purpose of synchronizing master and slave node timers in a CAN bus network, and allow the realization of a deterministic time schedule for time-critical applications.
